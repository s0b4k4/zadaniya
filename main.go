package main

import (
    "fmt"
    "log"
)

func main() {
    fmt.Println("Starting Course Tasks Application...")
    fmt.Println("This project structure demonstrates Clean Architecture, gRPC/REST setups, and patterns like CQRS, Circuit Breaker, and Event Workers.")
    
    // Example start of grpc server
    // s := grpc.NewServer(grpc.UnaryInterceptor(grpc_handler.AuthInterceptor))
    // pb.RegisterGarageServiceServer(s, &server{})
    // log.Fatal(s.Serve(lis))
}
